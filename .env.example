# API URL path or backend server address
# Setting VITE_API_URL enables Live Mode (turning off local mock mode)
# Use /api to route through the local development server proxy configured in vite.config.ts
VITE_API_URL=/api
